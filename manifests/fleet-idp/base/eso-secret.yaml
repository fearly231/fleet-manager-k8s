apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: fleet-idp-secrets-es
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: fleet-idp-secrets
  dataFrom:
  - extract:
      key: dev-fleet-idp-secrets
